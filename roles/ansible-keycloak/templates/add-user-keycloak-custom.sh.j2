#!/bin/sh

FILE=/var/lib/keycloak/standalone/configuration/keycloak-add-user.json

if [ ! -f "$FILE" ]; then
    {{ keycloak_link_dir }}/bin/add-user-keycloak.sh -u {{ keycloak_admin }} -p {{ keycloak_password }}
fi